version: 1

task:
  name: "web-habr-tech"
  description: "Habr articles via RSS (tech keywords)"

time:
  lookback_hours: 168

sources:
  web:
    sites:
      - site: "habr.com"
        feed_url: "https://habr.com/ru/rss/articles/"

filters:
  include_keywords:
    - intel
    - amd
    - nvidia
    - cpu
    - gpu
    - память
    - ddr5

output:
  max_items: 10
